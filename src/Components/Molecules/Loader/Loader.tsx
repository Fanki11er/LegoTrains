import { Html } from "@react-three/drei";
import { LoaderWrapper } from "./Loader.styles";

const Loader = () => {
  return (
    <Html>
      <LoaderWrapper>
        <div>Loading</div>
      </LoaderWrapper>
    </Html>
  );
};

export default Loader;
